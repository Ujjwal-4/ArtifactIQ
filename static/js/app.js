const modal=document.getElementById('modal'),title=document.getElementById('modalTitle'),text=document.getElementById('modalText'),kicker=document.getElementById('modalKicker'),body=document.getElementById('modalBody');
const modes={live:{kicker:'01 · LIVE ACQUISITION',title:'Run forensic collection on this system',text:'ArtifactIQ will eventually collect authorized artifacts such as Windows Event Logs, Registry evidence, USB history, browser data, login activity and file metadata. This prototype only establishes the workflow entry point.',body:'<div class="modal-actions"><button class="primary" type="button">Continue to case setup</button><button type="button" class="cancel">Cancel</button></div>'},image:{kicker:'02 · DISK IMAGE',title:'Analyze a forensic image',text:'Select a forensic image for read-oriented examination. The next module will validate the image, calculate source hashes and begin artifact parsing without modifying the evidence source.',body:'<div class="modal-actions"><button class="primary" type="button">Choose .dd / .E01</button><button type="button" class="cancel">Cancel</button></div>'},targeted:{kicker:'03 · TARGETED CHECK',title:'Choose a focused forensic capability',text:'Run only the evidence collector you need. This keeps small investigations fast and lets an examiner validate a single artifact source before running a complete case.',body:'<div class="target-options"><button class="target-option">Windows Event Logs</button><button class="target-option">Registry</button><button class="target-option">USB Activity</button><button class="target-option">Browser History</button><button class="target-option">File System</button><button class="target-option">Login Activity</button></div>'}};
function closeMode(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}function openMode(m){const d=modes[m];kicker.textContent=d.kicker;title.textContent=d.title;text.textContent=d.text;body.innerHTML=d.body;modal.classList.add('open');modal.setAttribute('aria-hidden','false');body.querySelectorAll('.cancel').forEach(b=>b.addEventListener('click',closeMode))}document.querySelectorAll('.mode-card').forEach(c=>c.addEventListener('click',()=>openMode(c.dataset.mode)));document.getElementById('closeModal').addEventListener('click',closeMode);modal.addEventListener('click',e=>{if(e.target===modal)closeMode()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMode()});
